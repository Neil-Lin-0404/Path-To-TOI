# Knapsack DP Thinking（費曼版）

> 流程永遠一樣：**純暴力 → 哪個小問題一直重複 → 填表 → 只留 1 個 CP 技巧。**

對照碼：
- 0/1 背包：[CSES/BookShop.cpp](CSES/BookShop.cpp)
- 完全背包：你已經會的 [CSES/CoinCombination2_BU.cpp](CSES/CoinCombination2_BU.cpp)
- loop order：[Bottom-Up-DP-Thinking.md](Bottom-Up-DP-Thinking.md) 技巧 2

---

## 背包在幹嘛

一個容量 `W` 的袋子 + 一堆物品。每件有重量、有價值。

問：袋子裡價值最大能多少（或有幾種湊法、最少幾件……問法可以換，盒子還是那些盒子）。

**0/1 跟完全，差只有一句話：同一件能不能拿第二次。**

---

## 0/1 背包：每件最多一次

例子：`W=5`，兩件 `(重量,價值) = (2,3), (3,4)`。

### 1. 暴力（沒技巧）

每件只有兩條路：拿 / 不拿。n 件就是 `2^n` 條路徑。

```
f(i, left) =
    不拿 i：f(i+1, left)
    拿 i：  f(i+1, left - weight[i]) + value[i]   // left 夠才行
取 max
```

人手枚舉：

- 都不拿 → 0
- 只拿第一件 → 3
- 只拿第二件 → 4
- 兩件都拿 → 重量 5，價值 **7**

答案 7。n 一到 40，這棵樹就爆了。

### 2. 痛點

很多條路徑會問到同一個問題：

> 現在輪到第 i 件，袋子還剩 left，最大價值是多少？

一樣的 `(i, left)` 被算很多次。這就是小問題。

### 3. optimize：改成填表

`dp[i][w]` = 只考慮前 i 件，容量剛好 ≤ w，最大價值。

```
dp[i][w] = max(
    dp[i-1][w],                      // 不拿第 i 件
    dp[i-1][w-weight[i]] + value[i]  // 拿（w 夠才行）
)
```

`dp[0][*] = 0`（一件都還沒考慮）。

手算（列是容量 0..5）：

| 考慮到哪 | dp | 意思 |
|---|---|---|
| 0 件 | `[0, 0, 0, 0, 0, 0]` | 空袋 |
| 第 1 件 (2,3) | `[0, 0, 3, 3, 3, 3]` | 容量 ≥2 就能拿它一次 |
| 第 2 件 (3,4) | `[0, 0, 3, 4, 4, 7]` | w=5 時：不拿=3，拿=3+4=7 |

答案 `dp[2][5] = 7`。跟暴力一樣，但每個 `(i,w)` 只算一次。

### 4. CP 技巧：壓成 1D，容量從大走到小

2D 的 `dp[i][*]` 只抄 `dp[i-1][*]`，所以可以只用一排盒子。

**但內層方向變性命攸關。**

同一件 `(2,3)`，`W=5`，從全 0 開始：

**大 → 小（0/1，對的）**

```
for w = 5..2:
    dp[w] = max(dp[w], dp[w-2] + 3)
```

| w | 用到的 dp[w-2] | 結果 |
|---|---|---|
| 5 | 還是 0（這輪還沒動過） | 3 |
| 4 | 還是 0 | 3 |
| 3 | 還是 0 | 3 |
| 2 | 還是 0 | 3 |

得到 `[0, 0, 3, 3, 3, 3]`。同一件只進一次。

**小 → 大（錯，變成完全背包）**

| w | 用到的 dp[w-2] | 結果 |
|---|---|---|
| 2 | 0 | 3 |
| 3 | 0 | 3 |
| 4 | **dp[2] 這輪剛變成 3** | 3+3=**6** |
| 5 | dp[3]=3 | 6 |

得到 `[0, 0, 3, 3, 6, 6]`。重量 4 卻把同一件拿了兩次。這就是 0/1 被寫成完全。

費曼直覺：

> 大→小：你往後看的小盒子，還是「沒放這件」的舊世界。  
> 小→大：你往後看的小盒子，可能這輪已經放進這件了 → 再放一次。

對照碼就是 Book Shop：外層書，內層錢**從大到小**。

---

## 完全背包：每件能拿無限次

### 1. 暴力（沒技巧）

每件拿 `0,1,2,...` 次，直到袋子裝不下。比 0/1 更爆。

### 2. 痛點

一樣會重複問「輪到第 i 種、還剩 w」。

### 3. optimize：你已經會了

這就是 Coin Combinations II：

```
for coin:                 // 外層物品
    for x = coin..target: // 內層容量，小 → 大
        dp[x] += dp[x-coin]
```

小→大 **故意**讓同一件再被拿。Min Coins 也是完全背包，只是問最少枚，不是問幾種。

價值版完全背包只是把 `+=` 換成 `max(..., dp[w-weight]+value)`，迴圈方向不變。

### 4. 一句話對照（這次唯一要背的）

```
同一組迴圈：外層物品、內層容量
內層小 → 大 = 完全背包（你的 coin）
內層大 → 小 = 0/1 背包（Book Shop）
```

外層為什麼是物品？因為順序不算，購物清單。技巧二講過了。  
新招只在**內層方向**：要不要讓「這一件」在這一輪被用第二次。

---

## 跟你現在這題切開

[CSES/RemovingDigits.cpp](CSES/RemovingDigits.cpp) **不是背包**，也不是序列 DP。

它是：數字 n 每次減掉一個位數，最少幾步到 0。小問題是 `dp[n]`，比較像 Min Coins（往後看較小的數）。

---

## 比賽怎麼選

1. 先問：同一件能不能拿第二次？能 → 完全；不能 → 0/1。
2. 先寫暴力 `f(i, left)`，確認拿 / 不拿（或拿幾次）。
3. 再填表。要壓 1D 時，只改內層方向。
