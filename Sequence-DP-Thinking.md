# Sequence DP Thinking（費曼版）

> 流程永遠一樣：**純暴力 → 哪個小問題一直重複 → 填表。**  
> 序列 DP 最容易反直覺的點：小問題是掛在「陣列的位置」上，而且有兩種掛法。

---

## 先切開：什麼才叫序列 DP

序列 DP：答案長在陣列 `a[0..n-1]` 的**位置**上。你問的是前幾個、或以誰結尾。

**不是序列 DP：** [CSES/RemovingDigits.cpp](CSES/RemovingDigits.cpp)  
那題小問題是數字本身 `dp[n]`（減掉位數），比較像 Min Coins，不是「第 i 個位置」。

---

## 兩種小問題（先分清，再寫公式）

| 型 | 小問題在問什麼 | 答案通常在哪 |
|---|---|---|
| **前綴型** | 只用 `a[0..i]`，**不一定**用到 `a[i]` | `dp[n-1]` |
| **結尾型** | **必須**以 `a[i]` 當結尾 | `max(dp[0..n-1])` |

同一個陣列，問法不同，公式就不同。下面各走一次暴力 → optimize。

---

## 前綴型：房子不能偷相鄰（House Robber）

一排房子，`a[i]` = 價值。不能偷相鄰兩棟。最大能偷多少。

### 1. 暴力（沒技巧）

每個房子偷 / 不偷，但不能跟前一棟都偷 → `2^n` 條路。

```
f(i) = max(
    不偷 i：f(i-1),
    偷 i：  f(i-2) + a[i]
)
```

例：`a = [1, 2, 3, 1]`

人手幾條路：

- `1,3` → 4
- `1,1`（頭尾）→ 2
- `2,1` → 3
- 只偷 `3` → 3

答案 4。

### 2. 痛點

「前 i 棟能偷的最大」一直被問。它只依賴前 i-1、前 i-2，跟更前面的具體選法無關。

### 3. optimize：改成填表

`dp[i]` = 只用前 i+1 棟（`a[0..i]`）的最大價值。**不保證偷了 i。**

```
dp[0] = a[0]
dp[1] = max(a[0], a[1])
dp[i] = max(dp[i-1], dp[i-2] + a[i])
```

手算 `[1, 2, 3, 1]`：

| i | a[i] | dp[i] | 怎麼來 |
|---|---|---|---|
| 0 | 1 | 1 | 只這棟 |
| 1 | 2 | max(1,2)=2 | 不偷這棟 / 偷這棟 |
| 2 | 3 | max(2, 1+3)=4 | 不偷 3 / 偷 3（前面只能接到 i=0） |
| 3 | 1 | max(4, 2+1)=4 | 不偷尾 / 偷尾 |

答案 `dp[3]=4`。就是暴力那棵樹，每個位置只算一次。

費曼直覺：走到第 i 棟，你只決定「這棟偷不偷」。不偷就繼承前綴最佳；偷就只能接「前一棟沒被強迫用到」的前綴，也就是 `dp[i-2]`。

---

## 結尾型：最長遞增子序列 LIS O(n²)

從陣列挑一個**不必連續**的遞增子序列，最長多長。

例：`a = [3, 1, 2, 4]` → `[1,2,4]`，長度 3。

### 1. 暴力（沒技巧）

每個位置選 / 不選，再檢查選出來的是否遞增 → `2^n`。

n=20 就炸。而且你會發現：很多子集前半段一樣，只是結尾不同。

### 2. 痛點

真正重複的小問題不是「前 i 個的 LIS」（那是前綴型，轉移會很煩）。

而是：

> **必須以位置 i 當結尾**的最長遞增，有多長？

它只看左邊：`j < i` 且 `a[j] < a[i]`，那些 `j` 的答案。

為什麼不是前綴型？  
因為新來的 `a[i]` 不一定接得上「前 i-1 的那條最長」。前綴最佳可能結尾很大，接不上；一條比較短、結尾比較小的反而接得上。所以小問題必須記住「以誰結尾」。

### 3. optimize：改成填表

`dp[i]` = **以 i 結尾**的 LIS 長度。

```
dp[i] = 1                          // 至少自己一個
for j = 0..i-1:
    if a[j] < a[i]:
        dp[i] = max(dp[i], dp[j] + 1)

答案 = max(dp[0], dp[1], ..., dp[n-1])
```

不是 `dp[n-1]`。因為最長那條不一定以最後一個數結尾。

手算 `[3, 1, 2, 4]`：

| i | a[i] | 能接誰 | dp[i] | 一條實際序列 |
|---|---|---|---|---|
| 0 | 3 | 沒人 | 1 | `[3]` |
| 1 | 1 | 3 不夠小 | 1 | `[1]` |
| 2 | 2 | 只能接 1 | 2 | `[1,2]` |
| 3 | 4 | 3、1、2 都能接 | 1+max(1,1,2)=**3** | `[1,2,4]` |

答案 `max(1,1,2,3)=3`。

如果誤用前綴型，以為 `dp[2]` 已經是「前三個的 LIS=2」，直接拿去加 `a[3]`，這次碰巧對；換成 `[3, 4, 1]` 就會錯：前綴 LIS 是 `[3,4]` 結尾 4，接不上後面的思路，而「以 1 結尾」其實只有 1。結尾型不會混。

---

## 比賽怎麼選

1. 先寫暴力：每個位置選或不選（或偷或不偷）。
2. 問自己：小問題是「前 i 個整體最佳」，還是「必須用 i 當結尾」？
3. 前綴型 → 答案在最後一格；結尾型 → 答案是整排 max。
4. LIS 先寫 O(n²)。`n log n` 以後再學，那是優化轉移，不是新定義。

對上背包：背包的小問題掛在「物品 i + 容量 w」；序列的小問題掛在「位置 i」。都是暴力樹上重複的那個問句，只是掛的軸不一樣。
