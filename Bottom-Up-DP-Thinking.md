# Bottom-Up DP Thinking（費曼版）

> 假設你只會 Top-Down。Bottom-Up 不是新算法，只是「同一句公式，改成從小填到大」。

對照碼：
- [CSES/MinimizingCoins_BU.cpp](CSES/MinimizingCoins_BU.cpp)
- [CSES/CoinCombination1_BU.cpp](CSES/CoinCombination1_BU.cpp)
- [CSES/CoinCombination2_BU.cpp](CSES/CoinCombination2_BU.cpp)
- 你已經會的 BU：[CSES/DiceCombination.cpp](CSES/DiceCombination.cpp)

---

## 先忘掉專有名詞

想像地上有一排盒子：`0, 1, 2, ..., target`。

`dp[x]` = 盒子 x 裡寫的答案。

你已經會的 Top-Down：

> 站在某個盒子前面，問旁邊的盒子：「你的答案是多少？」問過的貼便利貼，下次不用再問。

Bottom-Up：

> 從最簡單的盒子開始寫。盒子 0 你一定知道。寫完小的，大的就能抄小的來算。一路寫到 target。

公式一模一樣。只是 TD 從大問到小；BU 從小寫到大。

你的 Dice Combinations 已經是 BU：先知道 `dp[0]`，再算 `dp[1]`、`dp[2]`……

---

## 技巧 1：往前喊 vs 往後看

填盒子只有兩種手勢。

**往後看（Pull）**：填 `dp[x]` 時，回頭看比較小的盒子。

Min Coins：要湊 x 元，最少幾枚？

> 看 `x-1`、`x-5`、`x-7`，誰比較小，就 +1。

所以迴圈是：`x` 從小到大，每個 x 往後看。

**往前喊（Push）**：剛寫完 `dp[x]`，立刻告訴它能走到的大盒子。

Coin Comb I：有幾種「有順序」的方法湊 x？

> 我已經知道湊出 x 有 `dp[x]` 種。再丟一枚 coin，就能湊出 `x+coin`。把這個消息加進去。

所以迴圈是：寫完 x，就去更新 `x+coin`。

口訣：

- TD 遞迴是 `f(cur - coin)` → BU 往後看
- TD 遞迴是 `f(cur + coin)` → BU 往前喊

你的 Min Coins TD 是往小走 → 用往後看。  
你的 Comb I TD 是往大走 → 用往前喊。

---

## 技巧 2：誰在外層迴圈（最值錢，也最反直覺）

兩題公式幾乎一樣：`dp[x] += dp[x-coin]`。  
反直覺的點：**不是公式在算順序，是「你允許什麼時候再拿另一種硬幣」。**

先用最小例子，手算就懂。

硬幣 `{1, 2}`，目標 `3`。

人腦答案：

- 順序不算（購物清單）：`{1,1,1}`、`{1,2}` → **2 種**（Comb II）
- 順序算（時間線）：`1+1+1`、`1+2`、`2+1` → **3 種**（Comb I）

`2+1` 跟 `1+2` 用的硬幣一模一樣。差只在「2 有沒有被允許出現在 1 前面」。

---

### 故事 A：外層是金額 = 你在寫時間線（Comb I）

```
for x = 0..3:
    for coin in {1, 2}:
        dp[x+coin] += dp[x]
```

意思：現在口袋已經有 `x` 元，**下一秒可以接任何一種硬幣**。

手算（`dp[0]=1`，其餘 0）：

| 做到哪 | dp | 剛發生什麼 |
|---|---|---|
| 開始 | `[1, 0, 0, 0]` | 空口袋 1 種 |
| x=0 加 1、加 2 | `[1, 1, 1, 0]` | 做出 `1`、做出 `2` |
| x=1 加 1、加 2 | `[1, 1, 2, 1]` | `1+1`；以及 `1+2` |
| x=2 加 1 | `[1, 1, 2, 3]` | 從「做出 2 的兩種」再加 1：`1+1+1`、`2+1` |

`dp[3]=3`。

`1+2` 從哪來？口袋先有 1，再接 2。  
`2+1` 從哪來？口袋先有 2，再接 1。

因為每個金額階段，兩種硬幣都還能用，時間線被記下來了。

這就是你 Comb I 的 TD：`f(cur)` 裡 for 所有 coin，沒有 `index`。隨時都能拿任何硬幣。

---

### 故事 B：外層是硬幣 = 你在寫購物清單（Comb II）

```
for coin in {1, 2}:
    for x = coin..3:
        dp[x] += dp[x-coin]
```

意思：硬幣種類從左走到右。**先決定 1 要用幾枚，用完才輪到 2。用了 2 就不能再回去拿 1。**

手算：

**第 1 輪，只准用 1**

| x | 計算 | dp |
|---|---|---|
| 1 | `dp[1]+=dp[0]` → 一個 1 | `[1, 1, 0, 0]` |
| 2 | `dp[2]+=dp[1]` → 兩個 1 | `[1, 1, 1, 0]` |
| 3 | `dp[3]+=dp[2]` → 三個 1 | `[1, 1, 1, 1]` |

現在每個盒子 = 「只用 1 能怎麼湊」。

**第 2 輪，才准開始用 2**（1 已經定案，不能再加 1）

| x | 計算 | 意義 |
|---|---|---|
| 2 | `dp[2]+=dp[0]` → 再加一種：一枚 2 | `dp[2]=2` |
| 3 | `dp[3]+=dp[1]` → 再加一種：一個 1 + 一枚 2 | `dp[3]=2` |

`dp[3]=2`，只有 `{1,1,1}` 和 `{1,2}`。

`2+1` 為什麼消失？  
因為加 2 的時候，不能再往後加 1。`{2,1}` 跟 `{1,2}` 是同一張購物清單，只會在「先放 1、再放 2」這條路上被造出來一次。

這就是你 Comb II 的 TD：

```
f(cur, index) = 再用這枚 coins[index] + 改用下一枚
```

`index` 只會變大，不會變小。BU 不開 `index` 那一維，改用外層 `for coin` 代替：處理完硬幣 i，就再也不回去。

內層 `x` 從小到大，則是「同一種硬幣可以拿很多枚」（完全背包）。

---

### 為什麼看起來反直覺

你眼睛看到的都是 `dp[x] += dp[x-coin]`，以為在算同一件事。

其實公式只說「加一枚硬幣」。  
**誰在外層，才是在規定：加完這枚之後，還能不能自由選下一種。**

- 外層金額：每個金額都還能選所有硬幣 → 時間線，順序算
- 外層硬幣：每種硬幣只經過一輪，過了不回頭 → 購物清單，順序不算

對上你的 TD：

| 你怎麼寫 TD | BU 外層該轉誰 |
|---|---|
| `f(cur)`，裡面 for 所有 coin | 外層金額（Comb I） |
| `f(cur, index)`，index 只往後 | 外層硬幣（Comb II） |

一句話：

```
外層硬幣 = 組合（順序不算）
外層金額 = 排列（順序算）
```

---

## 技巧 3：每次只做 4 步

不要想「我要寫 Bottom-Up」。照抄你 TD 的公式，做這 4 步：

1. 把 `f(...)` 那句公式寫下來（不要改）
2. 哪個盒子不用問別人？通常 `dp[0] = 0` 或 `1`
3. 大盒子要抄小盒子 → 所以小的先算
4. 用 for 從小吃到大，把 `return f(...)` 換成 `dp[...] = ...`

例：Dice = `dp[i] = dp[i-1]+...+dp[i-6]`，`dp[0]=1`，i 從 2 寫到 n。你已經會了。

---

## 比賽什麼時候才需要 BU

Memo 能過就先交。改 BU 是因為：

- `target = 1e6` 遞迴可能爆 stack
- 要把 2D 壓成 1D（Comb II 那種）
- 之後要在轉移上加速（前綴 min、單調隊列）

先會翻，再談優化。
